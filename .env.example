DATABASE_URL=postgresql://localhost/twinengine
PORT=3000
# Session secret for session-based auth
SESSION_SECRET=your-session-secret-here

# JWT secrets for token-based auth
JWT_SECRET=your-jwt-secret-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here

# Optional: Anthropic API key for GenAI-powered NL query processing
# Falls back to rule-based parsing if not set
ANTHROPIC_API_KEY=
# Dev mode: Allow skipping login on splash page (set to "true" to enable)
ALLOW_DEV_BYPASS=true

# Omni-Voice API URL (for chat widget integration)
VITE_OMNIVOICE_API_URL=http://localhost:8080

# Forma Predictive API (drug launch forecasting)
FORMA_API_URL=http://localhost:8001
FORMA_API_KEY=
